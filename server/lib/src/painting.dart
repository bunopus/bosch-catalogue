
class Painting {
  String title;
  List<String> tags;
  String description;
  String preview;
  String bigPreview;

  Painting(this.title, this.tags, this.description, this.preview, this.bigPreview);
}
